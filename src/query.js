/**
 * queries are run against data.
 *
 */
'use strict';

var matcher = require('document-matcher'),
	comparator = require('comparator');

/**
 *
 *
 *
 */
exports.query = function query(criteria, skip, limit, order) {
	// [1] Read data from files.

	// [2]
};
